prefix iisg: <https://iisg.amsterdam/vocab/>
prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#>
select ?street (count(?address) as ?addresses) {
  ?address iisg:street/rdfs:label ?street.
}
group by ?street
order by desc(?addresses)
